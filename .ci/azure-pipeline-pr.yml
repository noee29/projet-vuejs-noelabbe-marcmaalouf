trigger: none

pool:
  vmImage: 'ubuntu-latest'  # Ex̩cuter sur une VM Ubuntu
jobs:
  - job: Front
    variables:
      - group: 'cours-framework-dev'
    steps:  
      - template: ./front/azure-pipeline-build.yml